window.onload = function() {
  let boardColor = 


}

document.querySelector('')

// função "clickColorSelect", que irá adicionar um addeventlistener para quando clicar no mouse, gravar a cor que está no elemento clicado, para ser inserido do elemento que será clicado após

// deve existir uma função que verifica se o primeiro elemento está na cor preta, para depois randomizar as outras cores

// função para limitar o tamanho mínimo e máximo do board

// função que gera as cores da paleta aleatoriamente

// função que captura a entrada de valores do input e a partir dele, gera a tabela com largura e altura definidos. Ex.: se digitar "7", a tabela terá 7 linhas e 7 colunas;

function generateTable(value) {
  let tableValue = document.getElementById('board-size');
  let tableNewSize = tableValue.innerText;
}

//função que limpa toda a tabela, baseado no exercício da pirâmide
function erasePixelTable() {
  
}