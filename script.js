window.onload = function() {
  let myBoard = document.querySelector('#pixel-board');
  let palette = document.querySelector('#color-palette');
  let btnVQV = documento.querySelector('#generate-board');
  
  initialColor();

  function initialColor() {
    let firstColor = document.getElementsByClassName("color-selected");
    if (firstColor.style.fontSize = "35px") 
  }

